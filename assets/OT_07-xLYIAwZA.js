const e=`---
category: Notes
date: '2025-12-22'
title: Optimization Theory - Week 7
---

# 1. Concept of an Algorithm

## Algorithmic Map
An algorithm is an iterative process generating a sequence of points $\\mathbf{x}_1, \\mathbf{x}_2, \\dots$ according to a prescribed set of instructions.
*   **Map:** $\\mathbf{A}: X \\to X$ is generally a point-to-set map.
*   **Iteration:** $\\mathbf{x}_{k+1} \\in \\mathbf{A}(\\mathbf{x}_k)$.

## Solution Set $\\Omega$
The iterative procedure stops if it finds a point in the solution set $\\Omega$. Typical definitions of $\\Omega$ include:
*   Local optimal solutions.
*   Points satisfying $f(\\mathbf{x}) \\leq b$ (acceptable value) or $f(\\mathbf{x}) < LB + \\epsilon$.
*   Points satisfying Kuhn-Tucker or Fritz John optimality conditions.

## Convergence of Algorithms
Convergence is defined regarding the solution set $\\Omega$ rather than specific global optima.
*   **Definition:** The map $\\mathbf{A}$ converges over $Y \\subset X$ if, starting with any $\\mathbf{x}_1 \\in Y$, the limit of any convergent subsequence belongs to $\\Omega$.

## Closed Maps and Composition
*   **Definition 1.1 (Closed Map):** A map $\\mathbf{A}$ is closed at $\\mathbf{x}$ if $\\mathbf{x}_k \\to \\mathbf{x}$ and $\\mathbf{y}_k \\to \\mathbf{y}$ with $\\mathbf{y}_k \\in \\mathbf{A}(\\mathbf{x}_k)$ imply $\\mathbf{y} \\in \\mathbf{A}(\\mathbf{x})$.
    *   *Note:* Continuous functions are closed maps, but closed maps may not be continuous.
*   **Theorem 1.1 (Composition):** Let $\\mathbf{A} = \\mathbf{CB}$. If $\\mathbf{B}$ is closed at $\\mathbf{x}$, $\\mathbf{C}$ is closed on $\\mathbf{B}(\\mathbf{x})$, and a boundedness condition holds for $\\mathbf{y}_k$, then the composite map $\\mathbf{A}$ is closed at $\\mathbf{x}$.

## Theorem 1.2: Global Convergence Theorem
Let $\\mathbf{A}$ be a point-to-set map and $\\Omega$ be the solution set. The sequence $\\{\\mathbf{x}_k\\}$ is generated by $\\mathbf{x}_{k+1} \\in \\mathbf{A}(\\mathbf{x}_k)$.
If the following conditions hold:
1.  The sequence $\\{\\mathbf{x}_k\\}$ is contained in a **compact** subset of $X$.
2.  There exists a **continuous descent function** $\\alpha$ such that $\\alpha(\\mathbf{y}) < \\alpha(\\mathbf{x})$ if $\\mathbf{x} \\notin \\Omega$ and $\\mathbf{y} \\in \\mathbf{A}(\\mathbf{x})$.
3.  The map $\\mathbf{A}$ is **closed** over the complement of $\\Omega$.

**Then:**
*   Every convergent subsequence has a limit in $\\Omega$.
*   $\\alpha(\\mathbf{x}_k) \\to \\alpha(\\mathbf{x})$ for some $\\mathbf{x} \\in \\Omega$.
*   **Corollary 1.1:** If $\\Omega$ is a singleton $\\{\\bar{x}\\}$, the whole sequence converges to $\\bar{x}$.

## Order of Convergence
Let $\\{r_k\\}$ converge to $\\bar{r}$. The order of convergence $p$ and rate $\\beta$ are defined by:
$$ \\limsup_{k \\to \\infty} \\frac{|r_{k+1} - \\bar{r}|}{|r_k - \\bar{r}|^p} = \\beta < \\infty $$
*   **Linear Convergence:** $p = 1$ and $0 < \\beta < 1$.
*   **Superlinear Convergence:** $p > 1$, or $p = 1$ and $\\beta = 0$.

---

# 2. Unconstrained Optimization: Line Search

## General Approach
Given $\\mathbf{x}_k$, find a direction $\\mathbf{d}_k$ and a step size $\\lambda_k$ such that $\\mathbf{x}_{k+1} = \\mathbf{x}_k + \\lambda_k \\mathbf{d}_k$.
*   The step size $\\lambda_k$ is chosen to minimize $\\theta(\\lambda) = f(\\mathbf{x}_k + \\lambda \\mathbf{d}_k)$.

## Interval of Uncertainty
*   **Definition 2.1:** An interval $[a, b]$ is an interval of uncertainty if a minimum point $\\bar{\\lambda}$ lies in $[a, b]$.
*   **Theorem 2.1:** Let $\\theta$ be convex on $[a, b]$. If $\\lambda < \\mu$:
    *   If $\\theta(\\lambda) > \\theta(\\mu)$, the minimum is in $[\\lambda, b]$.
    *   If $\\theta(\\lambda) \\leq \\theta(\\mu)$, the minimum is in $[a, µ]$.

## Search Methods without Derivatives
1.  **Dichotomous Search:**
    *   Evaluates two points $\\lambda_k, \\mu_k$ very close to the midpoint ($ \\frac{a_k+b_k}{2} \\pm \\epsilon $).
    *   Reduction factor per iteration: $\\approx 1/2$.
2.  **Golden Section Method:**
    *   Uses the ratio $\\alpha \\approx 0.618$ (solution to $\\alpha^2 + \\alpha - 1 = 0$).
    *   Points: $\\lambda_k = a_k + (1-\\alpha)(b_k - a_k)$ and $\\mu_k = a_k + \\alpha(b_k - a_k)$.
    *   Reduction factor: $0.618$.
3.  **Fibonacci Search:**
    *   Uses Fibonacci numbers $F_{v+1} = F_v + F_{v-1}$.
    *   Provides the optimal reduction for a fixed number of evaluations.

## Search Methods Using Derivatives
1.  **Bisecting Search Method:**
    *   Evaluates derivative $\\theta'(\\lambda)$ at the midpoint.
    *   If $\\theta' > 0$, optimal is to the left; otherwise, to the right.
2.  **Newton's Method:**
    *   Approximates $\\theta(\\lambda)$ using a quadratic function $q(\\lambda)$.
    *   Update rule: $\\mathbf{x}_{k+1} = \\mathbf{x}_k - \\frac{\\theta'(\\mathbf{x}_k)}{\\theta''(\\mathbf{x}_k)}$.

## Theoretical Results for Line Search
*   **Theorem 2.2 (Convergence of Newton's Method):** If the starting point is sufficiently close to the solution $\\bar{x}$ and specific conditions on the first and second derivatives are met, Newton's method converges to $\\bar{x}$.
*   **Theorem 2.3 (Closedness of Line Search Map):**
    *   Let $\\mathbf{M}(\\mathbf{x}, \\mathbf{d}) = \\{ \\mathbf{y} : \\mathbf{y} = \\mathbf{x} + \\bar{\\lambda}\\mathbf{d}, f(\\mathbf{y}) \\text{ is minimized} \\}$.
    *   If $f$ is continuous and $\\mathbf{d} \\neq 0$, then the map $\\mathbf{M}$ is **closed** at $(\\mathbf{x}, \\mathbf{d})$.
    *   This implies the overall algorithmic map $\\mathbf{A} = \\mathbf{MD}$ is closed (via Theorem 1.1), ensuring convergence under the conditions of Theorem 1.2.`;export{e as default};
