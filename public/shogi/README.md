# Shogi visual assets

The piece PNGs in `pieces/portella` and the paper-style SVGs in
`pieces/paper` are from `shogi-player` 1.1.33. The original package
license is included in `LICENSE`.

The board PNGs in `board` are also from that package. `board-wood.svg` is a
local fallback pattern created for this project.
